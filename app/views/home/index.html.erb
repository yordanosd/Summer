<div class="home-pg">
  <img src="/assets/group working out with logo" width="80%" >
  <% if !current_user %>
    <div class="center">
      <%= link_to(image_tag("/assets/register a team sign", :alt =>  "alt text", :class =>"btn"), "/register-a-team")%>
    </div>

  <% end %>
</div>
